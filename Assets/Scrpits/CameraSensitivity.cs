using Unity.Cinemachine;
using UnityEngine;

public class CameraSensitivity : MonoBehaviour
{
    /*public CinemachineFreeLook freeLookCamera;


    void Start()
    {
        // ustaw pocz¹tkow¹ wartoœæ Gain
        UpdateSensitivity(GlobalVariables.sensitivity);
    }

    void UpdateSensitivity(float value)
    {
        // Zmiana GAIN dla osi X
        freeLookCamera.m_XAxis.m_MaxSpeed = value;
        Debug.Log(freeLookCamera.m_XAxis.m_MaxSpeed);
        // Zmiana GAIN dla osi Y
        freeLookCamera.m_YAxis.m_MaxSpeed = value;
    }*/
    // Update is called once per frame

}
